name: Diag ARM Runner

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  test-amd64:
    runs-on: ubuntu-latest
    steps:
      - run: echo "amd64 OK $(uname -m)"

  test-arm64:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: echo "arm64 OK $(uname -m)"
